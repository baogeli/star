name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  IMAGE_NAME: registry.cn-hangzhou.aliyuncs.com/boa/star
  KUSTOMIZE_DIR: manifest/deploy/kustomize/overlays/develop

jobs:
  # 1. 代码检查和测试
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.24.1'

      - name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}

      - name: Run tests
        run: go test ./internal/... ./api/... ./utility/... -v

      - name: Build
        run: go build -o main .

  # 2. 构建并推送 Docker 镜像
  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Aliyun Registry
        uses: docker/login-action@v2
        with:
          registry: registry.cn-hangzhou.aliyuncs.com
          username: ${{ secrets.ALIYUN_USERNAME }}
          password: ${{ secrets.ALIYUN_PASSWORD }}

      - name: Extract version
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.IMAGE_NAME }}:${{ steps.version.outputs.VERSION }}

  # 3. 部署到 Kubernetes（需要公网可访问的 K8s 集群）
  # deploy:
  #   needs: build-and-push
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v3
  #
  #     - name: Install kubectl
  #       uses: azure/setup-kubectl@v3
  #
  #     - name: Configure kubeconfig
  #       run: |
  #         mkdir -p $HOME/.kube
  #         echo "${{ secrets.KUBE_CONFIG }}" > $HOME/.kube/config
  #
  #     - name: Update image in Kustomize
  #       run: |
  #         cd ${{ env.KUSTOMIZE_DIR }}
  #         kustomize edit set image ${IMAGE_NAME}=${IMAGE_NAME}:${{ github.sha }}
  #
  #     - name: Deploy to K8s
  #       run: kubectl apply -k ${{ env.KUSTOMIZE_DIR }}
  #
  #     - name: Verify deployment
  #       run: |
  #         kubectl rollout status deployment/boa-star --timeout=300s
