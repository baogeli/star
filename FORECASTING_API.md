# 基金预测看板 API 文档

## 📅 更新日志

### 2026-04-09 - v1.1 操作建议增强版

**后端变更**:
1. ✅ 修复数据查询逻辑:改为获取**最新30天**数据(之前是最早30天)
2. ✅ 丰富操作建议内容:增加多维度分析和emoji标识
3. ✅ 新增波动率分析:根据波动率给出不同建议

**前端需要同步修改**:
1. 🔄 操作建议字段 `suggestion` 现在包含更详细的分析信息
2. 🎨 建议使用emoji或图标区分信号强度
3. 📊 可展示波动率指标辅助决策

---

### 2026-04-09 - v1.0 初始版本

- 基础接口框架
- 持仓、价格走势、技术指标、趋势信号

---

## 接口信息

**接口路径**: `GET 127.0.0.1:8000/v1/investment/forecasting`  
**功能**: 获取基金持仓概览、价格走势、技术指标和趋势预测信号

---

## 返回数据结构

### 1. holdings - 持仓概览

每只基金的详细持仓信息。

| 字段 | 类型 | 说明 |
|------|------|------|
| fund_code | string | 基金代码 |
| fund_name | string | 基金名称 |
| shares | int | 持有份额 |
| cost_price | float64 | 成本价 |
| current_price | float64 | 当前价格 |
| market_value | float64 | 市值 (当前价 × 份额) |
| profit_loss | float64 | 盈亏金额 (市值 - 成本) |
| profit_rate | float64 | 盈亏率 (%) |

---

### 2. price_trends - 价格走势

按基金代码分组的最近30天历史数据，用于绘制K线图或折线图。

**结构**: `map[string][]FundData`

**FundData 字段**:
- `create_time`: 日期
- `current_price`: 收盘价
- `opening_price`: 开盘价
- `highest_price`: 最高价
- `lowest_price`: 最低价
- `volume`: 成交量
- `turnover`: 成交额

---

### 3. technical_indicators - 技术指标

每只基金的技术分析指标。

| 字段 | 类型 | 说明 | 备注 |
|------|------|------|------|
| ma5 | float64 | 5日均线 | 短期趋势参考 |
| ma20 | float64 | 20日均线 | 中期趋势参考 |
| change_5d | float64 | 5日涨跌幅 (%) | 短期动量 |
| change_20d | float64 | 20日涨跌幅 (%) | 中期动量 |
| avg_volume | int64 | 平均成交量 | 活跃度指标 |
| rsi | float64 | RSI相对强弱指标 (0-100) | <30超卖, >70超买 |
| volatility | float64 | 年化波动率 (%) | 风险程度 |

**RSI 解读**:
- **0-30**: 超卖区域，可能反弹
- **30-70**: 正常区间
- **70-100**: 超买区域，可能回调

---

### 4. trend_signals - 趋势预测信号 ⭐

基于多维度分析的趋势预测和交易建议。

| 字段 | 类型 | 说明 | 可选值 |
|------|------|------|--------|
| trend | string | 趋势方向 | uptrend(上涨) / downtrend(下跌) / sideways(震荡) |
| signal | string | 交易信号 | buy(买入) / sell(卖出) / hold(持有) |
| strength | string | 信号强度 | strong(强) / medium(中) / weak(弱) |
| confidence | float64 | 置信度 (0-100%) | 数值越高越可靠 |
| support_level | float64 | 支撑位价格 | 近期低点，价格下跌时的支撑 |
| resistance_level | float64 | 阻力位价格 | 近期高点，价格上涨时的阻力 |
| suggestion | string | 操作建议 | **含emoji和详细分析** (见下方示例) |

**预测逻辑**:
1. **均线系统**: MA5与MA20的金叉/死叉判断
2. **RSI指标**: 超买超卖状态
3. **价格位置**: 当前价相对均线的位置
4. **短期动量**: 5日涨跌幅
5. **支撑阻力**: 关键价位距离
6. **波动率分析**: 年化波动率评估风险

综合以上维度打分，得出买卖信号和置信度。

---

### 📝 操作建议示例 (suggestion 字段)

#### 🟢 强烈买入信号
```
🟢 强烈买入信号 | RSI=28.5(超卖区域) | 当前价8.270接近支撑位8.150 | 5日均线8.280 > 20日均线8.200 | 建议分批建仓,设置止损位
```

**触发条件**:
- RSI < 30 (超卖)
- 当前价接近支撑位
- MA5 > MA20 (金叉)

**前端展示**:
- 绿色背景卡片
- 显示所有技术指标
- 突出"分批建仓"建议

---

#### 🟡 中等买入信号
```
🟡 买入信号 | 5日均线8.280上穿20日均线8.250 | RSI=42.3(偏低) | 5日跌幅-1.25%(超跌反弹机会) | 可适量买入,控制仓位
```

**触发条件** (满足其一):
- MA5 上穿 MA20
- RSI < 45
- 5日跌幅 > 0 (超跌)

**前端展示**:
- 黄绿色背景
- 动态显示触发的条件
- 提示"控制仓位"

---

#### 🔴 强烈卖出信号
```
🔴 强烈卖出信号 | RSI=75.2(超买区域) | 当前价8.450接近阻力位8.500 | 5日均线8.420 < 20日均线8.450 | 建议减仓止盈,规避回调风险
```

**触发条件**:
- RSI > 70 (超买)
- 当前价接近阻力位
- MA5 < MA20 (死叉)

**前端展示**:
- 红色背景卡片
- 警示图标
- 突出"减仓止盈"建议

---

#### 🟠 中等卖出信号
```
🟠 卖出信号 | 5日均线8.420下穿20日均线8.450 | RSI=65.8(偏高) | 5日涨幅3.50%(短期过热) | 考虑部分止盈,锁定收益
```

**触发条件** (满足其一):
- MA5 下穿 MA20
- RSI > 60
- 5日涨幅 > 3%

**前端展示**:
- 橙红色背景
- 显示过热指标
- 提示"部分止盈"

---

#### ⚪ 震荡/持有信号

**RSI中性**:
```
⚪ 震荡行情 | RSI中性(45-55) | 建议持有观望,等待突破方向明确
```

**高波动**:
```
⚪ 高波动震荡 | 波动率35.2% | 建议持有,设置宽幅止损,避免频繁交易
```

**低波动**:
```
⚪ 低波动震荡 | 波动率8.5% | 建议持有,关注成交量变化,等待突破
```

**前端展示**:
- 灰色背景
- 根据波动率显示不同提示
- 建议"继续持有"

---

### 5. rankings - 排名对比

帮助快速识别表现最好/最差的基金。

| 字段 | 类型 | 说明 |
|------|------|------|
| by_profit_rate | []RankItem | 按持仓盈亏率排名 (降序) |
| by_change_5d | []RankItem | 按5日涨跌幅排名 (降序) |
| by_change_20d | []RankItem | 按20日涨跌幅排名 (降序) |

**RankItem 字段**:
- `fund_code`: 基金代码
- `fund_name`: 基金名称
- `value`: 数值 (盈亏率或涨跌幅)

---

### 6. summary - 组合汇总

整个投资组合的总体情况。

| 字段 | 类型 | 说明 |
|------|------|------|
| total_market_value | float64 | 总市值 |
| total_cost | float64 | 总成本 |
| total_profit_loss | float64 | 总盈亏 |
| total_profit_rate | float64 | 总盈亏率 (%) |

---

## 前端展示建议

### ⚠️ v1.1 版本前端同步修改指南

#### 1. 操作建议字段处理

**变更说明**: `suggestion` 字段现在包含emoji和更详细的分析信息

**之前**:
```javascript
// 简单文本
"强烈买入信号 | RSI=28(超卖) | 建议分批建仓"
```

**现在**:
```javascript
// 含emoji和多维度分析
"🟢 强烈买入信号 | RSI=28.5(超卖区域) | 当前价8.270接近支撑位8.150 | 5日均线8.280 > 20日均线8.200 | 建议分批建仓,设置止损位"
```

**前端适配方案**:

**方案A: 直接显示 (推荐)**
```jsx
// React 示例
<div className="suggestion">
  {signal.suggestion}
</div>
```
- ✅ 优点: 简单,保留完整信息
- ✅ emoji自动渲染
- ⚠️ 注意: 确保字体支持emoji

**方案B: 解析后结构化显示**
```javascript
// 解析 suggestion 字符串
function parseSuggestion(suggestion) {
  const parts = suggestion.split(' | ');
  return {
    signal: parts[0], // "🟢 强烈买入信号"
    rsi: parts[1],    // "RSI=28.5(超卖区域)"
    price: parts[2],  // "当前价8.270接近支撑位8.150"
    ma: parts[3],     // "5日均线8.280 > 20日均线8.200"
    advice: parts[4]  // "建议分批建仓,设置止损位"
  };
}
```
```jsx
// 结构化展示
<div className="suggestion-card">
  <div className="signal-header">{parsed.signal}</div>
  <div className="indicators">
    <span>{parsed.rsi}</span>
    <span>{parsed.price}</span>
    <span>{parsed.ma}</span>
  </div>
  <div className="advice">{parsed.advice}</div>
</div>
```
- ✅ 优点: 更灵活,可自定义样式
- ⚠️ 缺点: 需要解析逻辑

---

#### 2. 信号强度图标优化

**之前**:
```jsx
{signal.strength === 'strong' && '🔴🔴🔴'}
{signal.strength === 'medium' && '🟡🟡'}
{signal.strength === 'weak' && '⚪'}
```

**现在**: 直接使用 `suggestion` 中的emoji
```jsx
// 从 suggestion 提取第一个emoji
const emoji = signal.suggestion.match(/[\p{Emoji}]/u)?.[0] || '⚪';
<span className="signal-emoji">{emoji}</span>
```

或者根据 `signal` + `strength` 组合:
```jsx
const getSignalIcon = (signal, strength) => {
  const icons = {
    'buy-strong': '🟢',
    'buy-medium': '🟡',
    'buy-weak': '⚪',
    'sell-strong': '🔴',
    'sell-medium': '🟠',
    'sell-weak': '⚪',
    'hold': '⚪'
  };
  return icons[`${signal}-${strength}`] || '⚪';
};
```

---

#### 3. 新增波动率指标展示

**在技术指标卡片中添加**:
```jsx
<div className="indicator-item">
  <label>波动率</label>
  <span className={
    indicators.volatility > 30 ? 'high' :
    indicators.volatility < 10 ? 'low' : 'normal'
  }>
    {indicators.volatility.toFixed(1)}%
  </span>
</div>
```

**样式建议**:
```css
.indicator-item .high {
  color: #ff4d4f;  /* 红色 - 高风险 */
}
.indicator-item .low {
  color: #52c41a;  /* 绿色 - 低风险 */
}
.indicator-item .normal {
  color: #faad14;  /* 黄色 - 中等风险 */
}
```

---

#### 4. 数据查询逻辑变更影响

**重要**: 后端已修复为获取**最新30天**数据

**之前的问题**:
- 如果数据库有100条记录
- `OrderAsc + Limit(30)` 拿到的是第1-30条(最早的)

**现在的逻辑**:
- `OrderDesc + Limit(30)` 拿到最新的30条
- 然后反转为正序,方便前端画图

**前端无需修改**,因为:
- ✅ 数据结构不变
- ✅ 仍然是正序排列
- ✅ `price_trends[code][0]` 仍是最早的
- ✅ `price_trends[code][29]` 仍是最新的

**验证方法**:
```javascript
// 检查最后一条数据的日期是否是最近
const lastData = priceTrends['508008'][priceTrends['508008'].length - 1];
console.log('最新日期:', lastData.create_time); // 应该是最近的日期
```

---

#### 5. 测试清单

- [ ] 操作建议文本正常显示,emoji无乱码
- [ ] 不同强度的信号显示正确的颜色
- [ ] 波动率指标显示并着色
- [ ] 价格走势图显示的是最近30天数据
- [ ] 所有字段的数值精度正确 (小数点位数)

---

### 📊 布局方案

#### 1. 顶部卡片区域 (Summary)
```
┌─────────────────────────────────────────────┐
│  总市值: ¥XX,XXX  │  总盈亏: +¥X,XXX (+X%)  │
└─────────────────────────────────────────────┘
```
- 用颜色区分盈亏 (绿涨红跌)
- 大字体突出显示核心数据

---

#### 2. 持仓表格 (Holdings + Technical Indicators)

| 基金代码 | 基金名称 | 持有份额 | 当前价 | 市值 | 盈亏 | 盈亏率 | MA5 | MA20 | RSI |
|---------|---------|---------|-------|------|------|-------|-----|------|-----|
| 508008  | XXX     | 1000    | 8.27  | 8270 | +270 | +3.4% | 8.25| 8.20 | 55  |

**交互建议**:
- 点击行展开显示详细技术指标
- RSI用颜色标识: <30绿色(超卖), >70红色(超买)
- 盈亏率用红绿箭头标识涨跌

---

#### 3. 趋势信号卡片 (Trend Signals) ⭐

为每只基金显示一个信号卡片:

```
┌──────────────────────────────────┐
│  508008 - XXX基金                 │
│                                  │
│  趋势: 📈 上涨趋势                  │
│  信号: 🟢 买入 (强)                │
│  置信度: ████████░░ 75%           │
│                                  │
│  支撑位: ¥8.15  │  阻力位: ¥8.45   │
│                                  │
│  💡 强烈买入信号 | RSI=28(超卖)     │
│     当前价8.27接近支撑位8.15        │
│     建议分批建仓                   │
└──────────────────────────────────┘
```

**视觉设计**:
- **买入信号**: 绿色背景/边框
- **卖出信号**: 红色背景/边框
- **持有信号**: 灰色背景/边框
- **置信度**: 进度条展示
- **强度标识**: 
  - Strong: 🔴🔴🔴 三个点
  - Medium: 🟡🟡 两个点
  - Weak: ⚪ 一个点

---

#### 4. 价格走势图 (Price Trends)

使用图表库 (如 ECharts/Chart.js) 绘制:

**基础配置**:
- X轴: 日期 (`create_time`)
- Y轴: 价格 (`current_price`)
- 图例: 可选择显示不同基金

**进阶配置**:
- 叠加显示 MA5 和 MA20 均线 (虚线)
- 标注支撑位和阻力位 (水平线)
- K线图模式: 显示开盘/最高/最低/收盘
- 成交量柱状图 (底部副图)

**交互功能**:
- 鼠标悬停显示详细数据
- 缩放和平移
- 切换时间范围 (7天/30天/90天)

---

#### 5. 排名列表 (Rankings)

三个维度的排名表格:

**按盈亏率排名**:
```
排名  基金代码  基金名称  盈亏率
🥇   508008   XXX      +5.2%
🥈   180201   YYY      +3.1%
🥉   508033   ZZZ      +1.8%
4    508086   AAA      -0.5%
```

**按5日涨跌幅排名** 和 **按20日涨跌幅排名** 类似。

**用途**:
- 快速识别表现最好/最差的基金
- 辅助调仓决策

---

### 🎨 UI/UX 建议

1. **颜色规范**:
   - 上涨/盈利: #52c41a (绿色)
   - 下跌/亏损: #ff4d4f (红色)
   - 中性/持有: #8c8c8c (灰色)

2. **响应式设计**:
   - 桌面端: 多列布局
   - 移动端: 单列卡片式布局

3. **实时更新**:
   - 定时刷新 (每5分钟)
   - 显示最后更新时间

4. **数据加载状态**:
   - Skeleton 骨架屏
   - Loading 动画

5. **错误处理**:
   - 网络错误提示
   - 重试按钮

---

### 📱 移动端适配

**卡片式布局**:
```
[持仓卡片1]
  ├─ 基本信息
  ├─ 技术指标 (折叠)
  └─ 趋势信号 (折叠)

[持仓卡片2]
  ...

[走势图] (可横向滑动)

[排名列表] (Tab切换)
```

---

## 技术实现建议

### 推荐图表库

1. **ECharts** (推荐)
   - 功能强大，支持K线图
   - 中文文档完善
   - 示例: https://echarts.apache.org/examples/zh/index.html

2. **Chart.js**
   - 轻量级
   - 简单易用

3. **Ant Design Charts**
   - React 生态
   - 与企业UI框架集成好

### 示例代码 (ECharts)

```javascript
// 价格走势图
const option = {
  title: { text: '508008 价格走势' },
  xAxis: {
    type: 'category',
    data: dates // ['2026-04-01', '2026-04-02', ...]
  },
  yAxis: { type: 'value' },
  series: [
    {
      name: '收盘价',
      type: 'line',
      data: prices // [8.25, 8.27, ...]
    },
    {
      name: 'MA5',
      type: 'line',
      data: ma5Data,
      lineStyle: { type: 'dashed' }
    },
    {
      name: 'MA20',
      type: 'line',
      data: ma20Data,
      lineStyle: { type: 'dashed' }
    }
  ]
};
```

---

## 后续优化方向

1. **更多技术指标**:
   - MACD (平滑异同移动平均线)
   - Bollinger Bands (布林带)
   - KDJ (随机指标)

2. **高级预测模型**:
   - ARIMA 时间序列预测
   - LSTM 深度学习模型
   - 蒙特卡洛模拟

3. **基本面数据**:
   - 基金分红率
   - 底层资产运营状况
   - 行业对比

4. **风险提示**:
   - 最大回撤预警
   - 波动率异常提醒
   - 相关性分析

5. **个性化设置**:
   - 自定义预警阈值
   - 关注列表
   - 导出报表

---

## 注意事项

⚠️ **重要提示**:
1. 本接口提供的趋势预测信号仅供参考，不构成投资建议
2. 技术指标基于历史数据，无法保证未来表现
3. 投资有风险，决策需谨慎
4. 建议结合基本面分析和市场环境综合判断

---

## 更新日志

- **2026-04-09**: 初始版本，包含持仓、价格走势、技术指标、趋势信号
